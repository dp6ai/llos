= breadcrumbs

.project{:class => "padded"}
  = image_tag @project.image.url

  %h3=@project.title

  =textilize @project.description


  %p= link_to('Edit', edit_project_path(@project)) if logged_in?