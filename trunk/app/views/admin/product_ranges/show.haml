= breadcrumbs

.product_range{:class => "padded"}
  %h2= @product_range.name

  %p
    = image_tag @product_range.image.url

  %p= textilize_without_paragraph(@product_range.description)

  = link_to('Edit', edit_admin_product_range_path(@product_range)) if logged_in?
  = "/" if logged_in?
  = link_to 'Back', admin_product_ranges_path