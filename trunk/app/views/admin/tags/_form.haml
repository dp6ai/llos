= error_messages_for :tag

- form_for([:admin, @tag]) do |f|
  %p
    Name
    %br
    = f.text_field :name
  %p
    Kind
    %br
    = collection_select(:tag, :kind, [translate(:type),translate(:room),translate(:outdoor)], :to_s, :to_s, {:prompt => true})
  %p
    = f.submit "Submit"
