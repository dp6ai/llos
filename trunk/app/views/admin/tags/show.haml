= breadcrumbs

.tag{:class => "padded"}
  %p
    Name:
    =h @tag.name

  %p
    Kind:
    =h @tag.kind

  = "#{link_to('Edit', edit_admin_category_path(@tag))} /" if logged_in?
  = link_to 'Back', admin_categories_path